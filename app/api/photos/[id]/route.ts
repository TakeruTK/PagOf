import { env } from 'cloudflare:workers';
import { database,adminAllowed,failure } from '@/lib/server';
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;if(!/^[a-f0-9-]{36}$/.test(id))return new Response('Not found',{status:404});const visible=await database().prepare("SELECT id FROM pieces WHERE status='Publicado' AND images LIKE ? LIMIT 1").bind('%/api/photos/'+id+'%').first();if(!visible&&!await adminAllowed())return new Response('Not found',{status:404});const obj=await env.BUCKET?.get(id);if(!obj)return new Response('Not found',{status:404});return new Response(obj.body,{headers:{'Content-Type':obj.httpMetadata?.contentType||'image/jpeg','Cache-Control':'private, no-store','X-Content-Type-Options':'nosniff'}});}catch(e){return failure(e);}}
